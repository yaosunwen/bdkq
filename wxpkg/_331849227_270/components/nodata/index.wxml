<view class="no-data" style="{{'padding-top:'+e}}">
    <image alt="" src="{{b}}" wx:if="{{a}}"></image>
    <text wx:if="{{c}}">{{d}}</text>
    <text class="l_loading" wx:else>正在努力加载...</text>
</view>
